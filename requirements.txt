huggingface_hub>=0.24
pandas
pyarrow
tqdm
matplotlib
seaborn
scikit-learn
Pillow
mediapipe
python-dotenv
gdown  # scripts/dataset/download_wasd_subset.py — WASD's CSV annotations are only hosted on Google Drive
yt-dlp  # scripts/dataset/download_wasd_subset.py — WASD's source video pulled from YouTube (Drive zip hits a download quota)
# Default PyPI `torch` pulls the full GPU/CUDA build (several GB of bundled
# nvidia-* packages) — that's what's installed here. On a slow/constrained
# link, `pip install --index-url https://download.pytorch.org/whl/cpu torch`
# gets a much smaller CPU-only build instead (fine for dev/smoke-testing,
# not for real training runs).
torch
batch-face  # scripts/dataset/download_avspeech_subset.py — AVSpeech ships only a face-center hint,
            # not a box, so this project's own RetinaFace detection (GPU-batched) locates+tracks the face
